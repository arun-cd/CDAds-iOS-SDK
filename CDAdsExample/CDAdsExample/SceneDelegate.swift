import UIKit

class SceneDelegate: UIResponder, UIWindowSceneDelegate {

    var window: UIWindow?

    // UIKit creates the window and sets rootViewController from Main.storyboard
    // (UISceneStoryboardFile in Info.plist) before this is called.
}
